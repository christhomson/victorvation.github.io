extends layout

block append vars
  - bodyclass = 'article-detail'

block prepend title
  | #{ page.title + " - "}

block header
  include datetime
  h1.title
    a(href=page.url).internal
        | #{ page.title }
  p.date
    mixin datetime(page.date)

  //-
      p.author
        | #{ "Written by " }
        mixin author(page.metadata.author)

block content
  article.article
    section.content!= typogr(page.html).typogrify()
  h3 Comments
  include disqus

block prepend footer
  div.nav
    a(href=contents.index.url).internal « go back


